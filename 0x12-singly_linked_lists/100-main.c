#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0
 */
int main(void)
{
	printf("(A tortoise, having preety good sense of a hare's nature, challenges one to a race.)\n");
	return (0);
}
