#include "main.h"
#include <stdio.h>

/**
 * main - prints arguments both count and vector
 * @argc: prints the argument count
 * @argv: prints the argument vector
 *
 * Return: 0 always
 */

int main(int argc, char *argv[])
{
	(void) argv;
		printf("%d\n", argc - 1);

		return (0);
}
