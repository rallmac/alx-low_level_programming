#include <stdio.h>

/**
 * div - this function divides a variable with another
 * @a: the variable to be divided
 * @b: the variabe to divide
 * Return: the divided value
 */
int div(int a, int b)
{
	return (a / b);
}
