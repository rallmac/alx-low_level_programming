#include <stdlib.h>
#include <stdio.h>

/**
 * mul - this function adds two variables
 * @a: the first variable to be multiplied
 * @b: the variable with which multiply
 * Return: the multiplied value
 */
int mul(int a, int b)
{
	return (a * b);
}
