#include <stdlib.h>
#include <stdio.h>

/**
 * sub - this function subtracts one variable from another
 * @a: the variable from which to be subtracted
 * @b: the variable to subtract
 * Return: the added value
 */
int sub(int a, int b)
{
	return (a - b);
}
