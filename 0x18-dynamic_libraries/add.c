#include <stdlib.h>
#include <stdio.h>

/**
 * add - this function adds two variables
 * @a: the first variable to be added
 * @b: the variable with which to add
 * Return: the added value
 */
int add(int a, int b)
{
	return (a + b);
}
