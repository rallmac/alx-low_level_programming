#include <stdlib.h>
#include <stdio.h>

/**
 * mod - this function divides two variables
 * and returns the remainder
 * @a: the variable to be divided
 * @b: the variable with which to be divided
 * Return: the remainder
 */
int mod(int a, int b)
{
	return (a % b);
}
